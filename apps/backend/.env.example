# SQLite por padrão (local). Para Docker/Postgres, Compose sobrescreve.
DATABASE_URL="file:./dev.db"
FRONTEND_ORIGIN="http://localhost:4200"
NODE_ENV=development
XSRF_COOKIE_NAME=XSRF-TOKEN
XSRF_HEADER_NAME=X-XSRF-TOKEN
